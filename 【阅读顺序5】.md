```python
            if i >= k-1:
                result.append(nums[q[0]])
            # if i >= k:
            #     result.append(nums[i])

下面注释的为何不对？
为啥大于`k-1`就开始记录最大值了呢？
不应该大于`k`才行吗，因为大于`k`才形成窗口。
为何添加的是`nums[q[0]]`，`q[0]`不是下标吗？
我们要存的不是数据吗？
因此为何添加的不是`nums[i]`，但是`nums[i]`也是下标。
```
